# Manifold-consistent C/w VJP validation amendment

Status: **frozen before implementation and execution**. This amendment
preserves EVID-0029 through EVID-0035 and does not change the security
functional, fast gates, finite-difference step, Full center, or AP fallback.

## Root-cause resolution

The v2 synthetic failure is a valid fail-closed result. Its independent update
of `S_0` changes one sector while holding `S_1,S_2,S_3,p,z` fixed. Production
does not admit that direction: all four sectors are jointly generated by
`gram_moments._sectors(p,z)`. The residual-w identity is therefore not required
to remain inside its fast gate on an independent-sector path.

## Fixture A: spectral-only

Retain `S_s=0.00390625 I_64` only for spectral Fréchet checks: repeated and
clustered spectra, complex Hermitian directions, Loewner derivatives,
Hermiticity, finite gradients, and basis invariance. It must not validate C,
w, or downstream composition by independently moving sectors.

## Fixture B: C4-manifold consistent

For `k=0,...,63`, freeze `p_k=1/256`, `z_k=(k+1)+0i`, and `dz_k=z_k`.
The canonical float-hex payload SHA-256 is
`2e7fe7296e1b0d43f4303da36287163466558da15685d80eb3f2e1fe5e4b819f`.
At the existing frozen step `h=1e-6`, construct center and endpoints only as

`_sectors(p,z)`, `_sectors(p,z+h*dz)`, and `_sectors(p,z-h*dz)`.

All three pass the existing positivity, condition-number, reconstruction, and
residual-identity gates without override. The construction was selected solely
by those predeclared feasibility checks, before any analytic-versus-numerical
VJP comparison.

Future C/w and downstream validation must compare central differences along
this manifold-consistent prototype direction. `w` remains the production
residual norm. A non-fast endpoint is fail closed; reducing `h`, relaxing a
gate, or editing the fixture after derivative outcomes is prohibited.

## Claim boundary

A future PASS applies only to analytic Fréchet/VJP composition within this
algebraic complex128-fast domain. It does not validate Full-transmitter
PS/GS/VA training, AP differentiation, physical-transmitter performance,
publication training, or the security functional. The frozen Full center stays
mathematical-full-support, AP-only, and gradient-ineligible because its modes
are approximately `10^-675`.
