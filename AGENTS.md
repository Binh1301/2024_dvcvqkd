# Repository Agent Instructions

The primary active research project is:

`paper_uav_hap_cvqkd/`

Before performing any task that touches this project, read and obey:

`paper_uav_hap_cvqkd/AGENTS.md`

The project repository state, frozen specifications, implementation, tests,
and traceable result artifacts are authoritative. Previous chat/sidebar
history is optional context and must never be required to continue the
project.

Never perform publication-scale training, final-test access, final-test
evaluation, held-out publication evaluation, or another lifecycle-gated task
unless the project-level lifecycle state explicitly authorizes it and current
repository evidence independently supports that state.
